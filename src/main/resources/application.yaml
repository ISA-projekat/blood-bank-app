spring:
  datasource:
    url: jdbc:mysql://localhost:3308/bloodbank
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8080